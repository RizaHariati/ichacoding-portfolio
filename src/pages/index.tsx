import React from "react";
import Layout from "../components/layout/Layout";

type Props = {};

const index = (props: Props) => {
  return (
    <Layout>
      <div>
        <p className="font-poppins">Text index</p>
      </div>
    </Layout>
  );
};

export default index;
